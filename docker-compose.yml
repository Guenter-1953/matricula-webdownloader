version: "3"

services:
  matricula-downloader:
    build:
      context: .
      args:
        APP_VERSION: "1.1.2"
    container_name: matricula-downloader
    ports:
      - "5055:5000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
